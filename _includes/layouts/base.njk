<!doctype html>
<!--#if expr="$HTTP_COOKIE=/fonts\-loaded\=true/" -->
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en" class="fonts-loaded">
<!--#else -->
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">
<!--#endif -->

	{% include "components/head.njk" %}

	<body>

		<header>
			<em>
				<a href="{{ '/' | url }}">Technically <span class="ux">UX</span></a>
			</em>
			{% include "components/nav.njk" %}
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
